#include<stdio.h>
int factorial(int n){
	if(n==0){
		return (1);
	}
	else{
		return (n*factorial(n-1));
	}
}
int main(){
	int num,fact;
	printf("enter the number: ");
	scanf("%d",&num);
	fact=factorial(num);
	printf("factorial of the number: %d",fact);
}
